// sendFrames() is called from dashboard.html when capture completes.
// It posts an array of data URLs to /upload_frame and renders the returned JSON.
function sendFrames(){
  // This function is intentionally left as a placeholder when used from the
  // inline <script> in the dashboard template. The actual sendFrames is defined
  // in the template's <script> so we keep this file focused on utility-only
}
